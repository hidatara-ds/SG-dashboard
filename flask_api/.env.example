# Runtime
FLASK_ENV=production
NODE_ENV=production
PORT=5000

# Authentication (header: X-API-KEY)
API_KEY=changeme_backend_key

# External API (opsional, untuk kompatibilitas)
EXTERNAL_DB_API_URL=https://kedairekagreenhouse.my.id/api/latest-readings/HZ1
EXTERNAL_DB_API_KEY=changeme_external_key

# Database (Neon example; SSL wajib untuk Railway)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require

# Connection pool
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10